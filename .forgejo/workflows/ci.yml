on:
  push:
    branches: [master]
  pull_request:
jobs:
  test:
    runs-on: docker
    steps:
      - uses: actions/checkout@v4
      - name: Debug
        run: |
          echo "Hello from openglaze CI"
          ls -la
          python3 --version || echo "no python3"
